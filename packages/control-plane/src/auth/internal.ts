/**
 * Internal API authentication for service-to-service calls.
 *
 * Re-exports from @wrench/shared for consistency across packages.
 */

export { generateInternalToken, verifyInternalToken } from "@wrench/shared";
